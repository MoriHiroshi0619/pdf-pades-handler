services:
  app:
    build: .
    container_name: pades-handler
    env_file: .env
    ports:
      - "8005:8005"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8005/health"]
      interval: 60s
      timeout: 5s
      retries: 3
